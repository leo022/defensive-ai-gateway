# Optional. Leave empty for zero-configuration use on a trusted isolated LAN.
# Set a strong random value to require Bearer authentication for protected APIs.
DEFENSIVE_AI_API_TOKEN=

# Required only when using llm.provider: gateway.
DEFENSIVE_AI_LLM_API_KEY=

# Optional helper variables used by install-k3s-bundle.sh.
K3S_IMAGE_DIR=images
